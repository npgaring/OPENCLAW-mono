# Deterministic compiler stabilization controls
CODEGEN_CONFLICT_MODE=block
CODEGEN_STRICT_PACKAGE_PINNING_ENABLED=true

# Local Next.js builds: run `npm install` (or `npm install --legacy-peer-deps` if your tree requires it)
# in services/openclaw-integration or the generated repo before `npm run build`; otherwise `next` may be missing.
