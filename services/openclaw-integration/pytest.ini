[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    live: marks tests that call the real OpenClaw executor (deselect with -m "not live")
